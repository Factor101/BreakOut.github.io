const levels = {
  // [x min, x max, x add, y min, y max, y add, width, lives]
  1: [-500, 500, 150, -300, 50, 75, 100, 1],
};
/*
for( let i = -500; i <= 500; i += 150 )
  {
    for( let j = -300; j <= 50; j += 75 ) 
    {
      bricks.push(new brick(width / 2 + i, height / 2 + j, 100, 1));
    }
  }
  */
